name: 04-workflow
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  workflow_dispatch:
jobs:
  dev:
    runs-on: self-hosted
    steps:
      - name: Passo 01
        run:  echo  "Teste"
