name: Demo04
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]

workflow_dispatch: 
jobs:
  dev:
    runs-on: [ self-hosted, Linux ]
    steps:
      - name: Passo 01
        run:  echo  "Teste"
